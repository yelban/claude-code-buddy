{
  "$schema": "https://claude.ai/schemas/config.json",
  "mcpServers": {
    "memesh": {
      "command": "node",
      "args": [
        "/absolute/path/to/claude-code-buddy/dist/mcp/server-bootstrap.js"
      ],
      "env": {
        "LOG_LEVEL": "info"
      },
      "description": "MeMesh MCP server for persistent memory and context-aware task execution"
    }
  }
}
