# Claude Code Buddy - 環境變數配置範本 (V2.0 MCP Server Pattern)
# 複製這個文件為 .env 並填入必要配置
# ⚠️ 警告：.env 文件包含敏感信息，絕對不要提交到 Git！

# ====================================
# V2.0 MCP Server 配置（當前實現）
# ====================================

# MCP Server Mode
# 設為 true 時，ANTHROPIC_API_KEY 不是必需的（由 Claude Code 管理）
# 設為 false 時，必須提供 ANTHROPIC_API_KEY
MCP_SERVER_MODE=true

# MCP Server 運作環境
NODE_ENV=development
PORT=3000

# 日誌與監控
LOG_LEVEL=info  # debug, info, warn, error
ENABLE_METRICS=false  # 設為 true 可啟用性能監控

# Vector Database (Vectra - 本地 RAG)
# 如需使用 RAG agent，建議設定自訂路徑
# VECTRA_INDEX_PATH=~/.claude-code-buddy/vectra

# ====================================
# Embedding Provider 配置（RAG Agent）
# ====================================
# RAG Agent 使用 OpenAI Embeddings API（穩定可靠）
# 如未設定，使用 RAG 功能時會提示輸入 API key

# OpenAI Embeddings API
# 取得 API key: https://platform.openai.com/api-keys
# 成本：$0.02 / 1M tokens (~62,500 頁文本)
# OPENAI_API_KEY=sk-xxxxx
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small  # 可選，預設值

# ====================================
# Database Connection Pool 配置
# ====================================
# 連接池設定（提升並發查詢性能）
# DB_POOL_SIZE=5             # 最大連接數（預設：5）
# DB_POOL_TIMEOUT=5000       # 獲取連接超時（毫秒，預設：5000）
# DB_POOL_IDLE_TIMEOUT=30000 # 閒置連接超時（毫秒，預設：30000）

