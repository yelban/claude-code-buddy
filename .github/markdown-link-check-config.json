{
  "ignorePatterns": [
    {
      "pattern": "^http://localhost"
    },
    {
      "pattern": "^https://example.com"
    },
    {
      "pattern": "^https://www.npmjs.com",
      "comment": "npm website has anti-bot protection (403), but package exists and works"
    },
    {
      "pattern": "^https://www.w3.org/WAI/WCAG",
      "comment": "W3C rate limiting causes intermittent 403"
    },
    {
      "pattern": "^https://memesh.ai",
      "comment": "TODO: Remove when memesh.ai cloud service is deployed"
    }
  ],
  "replacementPatterns": [],
  "httpHeaders": [
    {
      "urls": ["https://github.com"],
      "headers": {
        "Accept-Encoding": "zstd, br, gzip, deflate"
      }
    }
  ],
  "timeout": "20s",
  "retryOn429": true,
  "retryCount": 3,
  "fallbackRetryDelay": "30s",
  "aliveStatusCodes": [200, 206, 999]
}
